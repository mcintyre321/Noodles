@using Noodles
@using System.Linq
@using Noodles.Helpers
@using Noodles.Web.Helpers
@model NodeActions
@if (Model.Any())
{
    foreach (var nodeMethod in Model)
    {
        @NoodlesHelper.NodeActionLink(nodeMethod)
    }
}
