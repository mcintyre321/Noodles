@using System.Web.Mvc.Html
@using WebNoodle
@model object
@if (Model.NodeMethods().Any())
{
    <a href="#loadactions" class="nodeActionsLink" data-nodeid="@Model.Id()" data-nodepath="@Model.Path()" >(edit)</a>
}
