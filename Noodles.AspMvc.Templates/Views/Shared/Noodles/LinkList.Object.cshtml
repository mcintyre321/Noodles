@using FormFactory
@model Noodles.Models.NodeCollectionProperty
<ul class="inline-children">
    @foreach(var item in Model.Items)
    {
        @Html.Partial("Noodles/Link.Object", item)
    }
</ul>

