@using Noodles
@using System.Linq
@using Noodles.Helpers
@using Noodles.AspMvc.Helpers
@model IEnumerable<Noodles.Models.NodeMethod>
@if (Model.Any())
{
    foreach (var nodeMethod in Model)
    {
        @NoodlesHelper.NodeMethodLink(nodeMethod)
    }
}
